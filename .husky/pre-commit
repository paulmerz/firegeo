branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "dev" ]; then
  echo "Running E2E on dev branch..."
  pnpm check|| exit 1
  pnpm test:e2e || exit 1

else
  echo "Skipping E2E (branch: $branch)"
fi
